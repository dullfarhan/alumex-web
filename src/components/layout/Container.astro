---
interface Props {
	class?: string;
	tag?: 'div' | 'section' | 'header' | 'footer' | 'nav';
}

const { class: className, tag: Tag = 'div' } = Astro.props;
---

<Tag class:list={['mx-auto w-full px-6 sm:px-12 py-12 md:py-16', className]}>
	<slot />
</Tag>
 